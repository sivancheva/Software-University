using System;
using System.Linq;

namespace _01.SumMatrixElements
{
    class Program
    {
        static void Main(string[] args)
        {
            var size = Console.ReadLine().Split(new char[] { ',', ' ' }).Select(int.Parse).ToArray();
            var rows = size[0];
            var columns = size[1];


        }
    }
}
